# Test-specific properties
spring.mongodb.uri=mongodb://localhost:27017/test-db
px-ledger.url=http://localhost:8081

# JWT Settings
jwt.secret=your-256-bit-secret-your-256-bit-secret-your-256-bit-secret
jwt.expiration=600000
jwt.refreshExpiration=604800000

# Disable the infinite loop listener during tests
px-gateway.listener.enabled=false
